<%= form_with(model: article) do |form| %>
  <% if article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(article.errors.count, "error") %> prohibited this article from being saved:</h2>
      <ul>
        <% article.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="container">
    <div class="row justify-content-md-center mt-4">
      <div class="col-6 ">
        <div class="card text-center">
          <div class="field card-header d-flex flex-column">
            <h4>
              <%= form.label :title %>
            </h4>
            <%= form.text_field :title %>
          </div>
          <div class="card-body">
            <div class="field card-title d-flex flex-column">
              <h4>
                <%= form.label :description %>
              </h4>
              <%= form.text_area :description %>
            </div>
            <div class="field card-title d-flex flex-column">
              <h4>
                <%= form.label :category_id%>
              </h4>
              <%= form.select :category_id, options_for_select(Category.all.map { |c| [c.category_name, c.id] }), class: "form-control"%>
            </div>
            <div class="actions tn btn-primary">
              <%= form.submit %>
            </div>
          </div>
          <div class="card-footer text-muted">
            2 days ago
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>
